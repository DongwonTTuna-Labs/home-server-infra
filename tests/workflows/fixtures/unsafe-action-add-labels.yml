name: Unsafe Add Labels Action

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  unsafe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions-ecosystem/action-add-labels@v1
        with:
          labels: codex-loop-active
