# Migration Status

Current Forgejo organization:

- `DongwonTTuna-Labs`

Current repositories:

- `bioden`: private
- `polymarket-liquidity-farming-rs`: private
- `rs-builder-relayer-client`: private

Current hostnames:

- Web: `https://git.dongwontuna.net`
- SSH: `git@ssh.dongwontuna.net:DongwonTTuna-Labs/<repo>.git`

Current status:

- Forgejo web works behind Cloudflare Access.
- Forgejo SSH works behind Cloudflare Access and WARP/Gateway bypass.
- Forgejo Actions runner is registered to the organization.
- Application workflow files remain in their application repositories.
- GitHub remains a legacy/archive remote during cutover.

